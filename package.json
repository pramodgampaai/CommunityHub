{
  "name": "elevate-community-management",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "react": "19.0.0",
    "react-dom": "19.0.0",
    "@supabase/supabase-js": "2.45.4",
    "framer-motion": "11.15.0",
    "jspdf": "2.5.1",
    "jspdf-autotable": "3.5.29",
    "esbuild": "0.24.0"
  },
  "overrides": {
    "framer-motion": {
      "react": "$react",
      "react-dom": "$react-dom"
    }
  },
  "scripts": {
    "build": "esbuild index.tsx --bundle --format=esm --outfile=dist/index.js --jsx=automatic --minify --external:react --external:react-dom --external:@supabase/supabase-js --external:framer-motion --external:jspdf --external:jspdf-autotable && cp index.html dist/ && (cp metadata.json dist/ || true)"
  }
}